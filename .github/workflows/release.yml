name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release, e.g. v0.4.0-beta (becomes the tag, the APK versionName and the asset names)'
        required: true
        type: string
      notes_file:
        description: 'Optional markdown committed in the repo to place above the generated changelog, e.g. release-notes/Release_Notes_0.4.0-beta.md (a bare filename is also looked up under release-notes/). Blank: use release-notes/Release_Notes_<version>.md if present. The What''s Changed changelog is always appended either way.'
        required: false
        type: string
        default: ''
      draft:
        description: 'Create as a draft so it can be reviewed before it goes public'
        type: boolean
        default: true
      prerelease:
        description: 'Mark as a pre-release'
        type: boolean
        default: true
      latest:
        description: 'Mark as the latest release (ignored for drafts)'
        type: boolean
        default: true

concurrency:
  group: release-${{ github.event.inputs.version }}
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      version: ${{ steps.check.outputs.version }}
    steps:
      - name: Validate inputs and preconditions
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail

          if ! printf '%s' "$VERSION" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9.]+)?$'; then
            echo "::error::version must look like v1.2.3 or v1.2.3-beta (got '$VERSION')"
            exit 1
          fi

          if gh release view "$VERSION" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "::error::release $VERSION already exists"
            exit 1
          fi

          if gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$VERSION" >/dev/null 2>&1; then
            echo "::error::tag $VERSION already exists"
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Releasing $VERSION from ${GITHUB_SHA}"

  build:
    needs: prepare
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: true
      matrix:
        flavor:
          - name: Standard
            gradleTask: assembleStandardDebug
            apkPath: app/build/outputs/apk/standard/debug/standard.apk
            artifactName: release-apk-standard

    steps:
      - name: Clone repository
        uses: actions/checkout@v6
        with:
          submodules: recursive
          fetch-depth: 1
          persist-credentials: false

      - name: Cache LFS
        uses: actions/cache@v5
        with:
          path: .git/lfs
          key: lfs-${{ hashFiles('**/*.txz', '**/*.tzst') }}
          restore-keys: lfs-

      - name: Pull LFS files
        run: git lfs pull

      - name: Cache NDK build outputs
        uses: actions/cache@v5
        with:
          path: |
            app/.cxx
            app/build/intermediates/cxx
          key: tag-ndk-${{ hashFiles('app/src/main/cpp/**', 'app/build.gradle', 'app/src/main/cpp/CMakeLists.txt') }}
          restore-keys: |
            tag-ndk-
            ndk-

      - name: Cache Rust Steam client
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            app/src/main/cpp/wn-steam-client/rust/target
          key: cargo-${{ hashFiles('app/src/main/cpp/wn-steam-client/rust/Cargo.lock') }}
          restore-keys: cargo-

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Rust Android target
        run: rustup target add aarch64-linux-android

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          validate-wrappers: true
          cache-cleanup: 'on-success'

      - name: Build release APK
        timeout-minutes: 20
        env:
          GRADLE_OPTS: -Xmx4g -XX:+UseG1GC
          VERSION_NAME: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          # Signing now comes from the repo-committed release.jks + signing.properties
          # (read directly by app/build.gradle's signingConfigs.release) — nothing to
          # decode here.
          ./gradlew :app:${{ matrix.flavor.gradleTask }} --build-cache --parallel -x lint

      - name: Verify version
        env:
          APK_PATH: ${{ matrix.flavor.apkPath }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          build_tools=$(find "$ANDROID_SDK_ROOT/build-tools" -maxdepth 1 -mindepth 1 -type d | sort -V | tail -1)

          version_name=$("$build_tools/aapt2" dump badging "$APK_PATH" |
            head -1 | grep -oE "versionName='[^']*'" | cut -d"'" -f2)
          if [ "$version_name" != "$VERSION" ]; then
            echo "::error::APK versionName is '$version_name', expected '$VERSION'"
            exit 1
          fi

          echo "versionName $version_name"

      - name: Prepare APK for upload
        env:
          APK_PATH: ${{ matrix.flavor.apkPath }}
          VARIANT_NAME: ${{ matrix.flavor.name }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          mkdir -p artifacts
          cp "$APK_PATH" "artifacts/WinLite-${VERSION}-${VARIANT_NAME}-signed.apk"

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.flavor.artifactName }}
          path: artifacts/*.apk
          retention-days: 7
          if-no-files-found: error
          compression-level: 0

  publish:
    needs: [prepare, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Clone repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Download built APKs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p assets
          for flavor in standard; do
            gh run download "$GITHUB_RUN_ID" \
              --repo "$GITHUB_REPOSITORY" \
              --name "release-apk-$flavor" \
              --dir assets
          done
          ls -l assets
          test "$(find assets -name '*.apk' | wc -l)" -eq 1

      - name: Assemble release notes
        id: notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ needs.prepare.outputs.version }}
          NOTES_FILE: ${{ inputs.notes_file }}
        run: |
          set -euo pipefail

          resolved=""
          if [ -n "$NOTES_FILE" ]; then
            for candidate in "$NOTES_FILE" "release-notes/$NOTES_FILE"; do
              if [ -f "$candidate" ]; then
                resolved="$candidate"
                break
              fi
            done
            if [ -z "$resolved" ]; then
              echo "::error::notes_file '$NOTES_FILE' not found in the repository at $GITHUB_SHA (looked at '$NOTES_FILE' and 'release-notes/$NOTES_FILE')"
              exit 1
            fi
          else
            for candidate in \
              "release-notes/Release_Notes_${VERSION}.md" \
              "release-notes/Release_Notes_${VERSION#v}.md" \
              "Release_Notes_${VERSION}.md" \
              "Release_Notes_${VERSION#v}.md"; do
              if [ -f "$candidate" ]; then
                resolved="$candidate"
                break
              fi
            done
          fi

          prev_tag=$(gh release list --repo "$GITHUB_REPOSITORY" --limit 1 \
            --exclude-drafts --json tagName --jq '.[0].tagName // ""')

          generate=(gh api --method POST "repos/$GITHUB_REPOSITORY/releases/generate-notes"
            -f "tag_name=$VERSION" -f "target_commitish=$GITHUB_SHA")
          if [ -n "$prev_tag" ]; then
            generate+=(-f "previous_tag_name=$prev_tag")
          fi

          if ! "${generate[@]}" --jq '.body' > generated.md; then
            echo "::warning::GitHub declined to generate release notes; falling back to commit subjects"
            : > generated.md
          fi

          changelog_line=$(grep -m1 '^\*\*Full Changelog\*\*' generated.md || true)
          if [ -z "$changelog_line" ]; then
            if [ -n "$prev_tag" ]; then
              changelog_line="**Full Changelog**: https://github.com/$GITHUB_REPOSITORY/compare/$prev_tag...$VERSION"
            else
              changelog_line="**Full Changelog**: https://github.com/$GITHUB_REPOSITORY/commits/$VERSION"
            fi
          fi

          if ! grep -q "^## What's Changed" generated.md; then
            echo "No pull requests in range; listing commit subjects instead"
            {
              printf "## What's Changed\n\n"
              if [ -n "$prev_tag" ] && git rev-parse -q --verify "refs/tags/$prev_tag" >/dev/null; then
                git log --pretty='* %s' "refs/tags/$prev_tag..HEAD"
              else
                git log --pretty='* %s' -50
              fi
              printf '\n%s\n' "$changelog_line"
            } > generated.md
          fi

          if [ -n "$resolved" ]; then
            echo "Prepending notes from $resolved"
            {
              awk '
                { lines[n++] = $0 }
                END {
                  while (n > 0 && (lines[n-1] == "" || lines[n-1] ~ /^\*\*Full Changelog\*\*/)) n--
                  for (i = 0; i < n; i++) print lines[i]
                }
              ' "$resolved"
              printf '\n'
              cat generated.md
            } > notes.md
          else
            mv generated.md notes.md
          fi

          echo "--- release notes ---"
          cat notes.md

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          flags=()
          if [ "${{ inputs.draft }}" = "true" ]; then flags+=(--draft); fi
          if [ "${{ inputs.prerelease }}" = "true" ]; then flags+=(--prerelease); fi
          if [ "${{ inputs.latest }}" = "true" ] && [ "${{ inputs.draft }}" != "true" ]; then
            flags+=(--latest)
          fi

          gh release create "$VERSION" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "$VERSION" \
            --notes-file notes.md \
            "${flags[@]}" \
            assets/*.apk

          url=$(gh release view "$VERSION" --repo "$GITHUB_REPOSITORY" --json url --jq '.url')
          echo "Released $VERSION -> $url"
          {
            printf '### Release %s\n\n' "$VERSION"
            printf '%s\n\n' "$url"
            printf 'Draft: %s | Pre-release: %s\n' "${{ inputs.draft }}" "${{ inputs.prerelease }}"
          } >> "$GITHUB_STEP_SUMMARY"
